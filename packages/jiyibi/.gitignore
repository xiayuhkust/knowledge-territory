# deps 由 profile/包管理器提供(本仓 node_modules 是指向 profile 库的软链,不入库)
node_modules/

# 账本落在用户 homedir(~/.dsh/jiyibi/marks.jsonl),不在本仓;这里再兜一层以防万一
*.jsonl

# 日志 / 临时
*.log
*.tmp

# OS / 编辑器
.DS_Store
Thumbs.db
.vscode/
.idea/
